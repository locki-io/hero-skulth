# hero-skulth — APPS concern (SECRET class by suffix; radius: apps)
# Copy to .env.apps; register in .forseti/key-registry.yaml (skulth side) before first run.
SKULTH_POD_WALLET_KEY=
SKULTH_API_KEY_POD=
SKULTH_API_SECRET_POD=
