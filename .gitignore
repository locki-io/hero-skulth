# --- sovereignty seal (skulth#5 M3) ---
.env
.env.*
!env-templates/
.forseti/
# wells & local state (never public)
.chroma/
.wells/
pod-activity.jsonl
*.log
# runtime
__pycache__/
*.pyc
.venv/
node_modules/
.pytest_cache/

# pod runtime state (tombstone, activity log) — never committed
state/

!env-templates/
!env-templates/.env.*
